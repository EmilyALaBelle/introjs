const age = 19
function isDrinkingAge(age) {
    if (age >= 21) {
        console.log('You can drink!')
    } else {
        console.log('You can not drink')
    }
}
isDrinkingAge(age)

const base = 12
const height = 24
function computeAreaOfTriangle(base, height) {
    return (base * height) * .5
    console.log()
}

area = computeAreaOfTriangle(base, height)
console.log(area)
